# 进阶与注意事项

---

## 增量发布策略

- 本地 JSON/MD = 源；production = `--missing` 新增 + manifest 驱动 patch
- 禁止无授权全量 convert + `--replace`（AB-P02）
- repair 去重：`~/lovart/repair-state.json`（AB-P06）

详见 Harness：`first-run-and-incremental-policy.md`。

---

## patch vs import 决策

| 状态 | 动作 |
|------|------|
| 线上无 `_id` | convert → import `--missing` |
| 线上有、字段错 | `patch-*.js --dry-run` → `--apply` |
| HTTP 404 图 | 先 audit JSON → `--only-broken` patch |

---

## SEO 进阶

- 月报 §4 movers、§11 大区、收录率口径 — 见 AGENTS Part A
- IndexNow：`1-4 Dev/scripts/multi_seo/indexnow_submit.py`
- Sitemap Skill：`lovart-sitemap-update`

---

## 内容进阶

- composite v2 白名单：`scripts/lib/legacy-to-composite.js`
- Blog 封面池 SSOT：`blog-cover-pool.js`（禁止 liblib 新稿，AB-I08）
- 故事线 SSOT：`1-3 Content Gen/Page Gen/Refresh-Page/`（Readme 下仅为浏览副本）

---

## Agent 使用注意

- 命令写**全路径**，禁止 `…/` 省略（AB-E01）
- 用户叫停后立即缩小为 `--only-broken` / `--limit`（AB-A03）
- 文档 SSOT 在 Readme；执行前读对应 Skill，不即兴改 schema

---

## 归档与搜索

- 审计报告：`1-7 Output/quality-audits/YYYY-MM/`
- Studio 历史副本：`1-8 Backup/archives/`（勿在 Readme 下搜 node_modules）
