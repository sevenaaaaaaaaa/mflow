---
type: session-log
session_date: 2026-09-16
session_slug: p7-b1-b3-gate
status: ready
---

# Session Log — P7 批次（衰减监测 / 多引擎 / 门禁固化）

## 背景
用户无 Perplexity Key，先跑 B1+B3+C。VERSION → 1.2.0。

## 交付（服务器 e2e 全过）

1. **B1 内容衰减监测**：`decay_analysis()`——已发布 ≥30 天 × GSC Top20 无记录 × 无 AI 引用（citations 全项目扫描 + URL 归一化）→ /api/impact.decayed（7 条真实命中）；分发页新增「内容衰减监测」卡，每条带「→ 改稿 Loop」（refresh-{slug}，brief 含衰减原因+GEO 重写标准）。**诚实边界**：GSC 数据是 Top20 子集，"无记录"≠"零点击"，reason 文案如实标注。
2. **B3 多引擎交叉探测**：`llm_chat_full(engine=<provider>)` 泛化（perplexity 特判→任意 provider 直连，缺 key 引擎级明确报错）；meta.geo.engines[]（≤4 引擎/查询交叉）；geo_summary 增 per_engine（n/brand）+ cross（双引擎查询数 / 分歧数）；GEO 卡引擎改多选 checkbox，缺口行显示每引擎 ✓/✗ 徽标 + 「引擎分歧」标记。probe = 查询 × 引擎矩阵，token 记账照常。
3. **C 门禁固化**：session-init 新增 **GATE 5 console syntax**——node --check 逐 script 块（console.html/login.html）+ ast.parse console.py + bash -n 全部 hook；node 缺失不误判失败。顺修门禁自身三 bug：裸 python3 → PY3 统一解析（LOVART_PYTHON→venv→系统，服务器 py3.6 ascii 读中文曾连坐 GATE2/3）；hooks glob 相对路径 → $MFLOW_ROOT 绝对路径；ast.parse 显式 utf-8。**服务器 5/5 PASS**。

## 踩坑
- session-init 在服务器上 GATE 2/3/5 全挂——根因与 smoketest 同款：脚本内 python3 未走 venv 解析 + 相对路径 glob。教训：**任何"服务器也要跑"的脚本，python 调用必须走 LOVART_PYTHON 链，glob 必须 $ROOT 前缀**
- 改 renderGeoSummary 时顺手覆盖掉了竞品提及行——patch 竞赛时逐行核对被替换段（同 v3.8 教训）

## Loop 闭环（自 P7 起）
```
每日探测（多引擎交叉）→ 引用记录 → 被引用页面
缺口 → 选题/改稿   衰减（30天×无GSC×无引用）→ 改稿 Loop
→ 自动排程生成（geo-check 门禁）→ 人工审 → 发布 → 归因 → …
```

## 验证
- impact.decayed 7 条（真实数据）✓；engines 配置持久化 ✓；probe 引擎级报错（auto=未配 Key / openai=未配 Key）✓
- session-init 本地+服务器 5/5 ✓；服务器版本 1.2.0 ✓
- 测试配置恢复为真实默认（Lovart/3 竞品/4 查询/auto）

## 下一步候选
- B2 竞品引用源反向工程（竞品被引 URL → 对标选题）
- B5 发布→7 天后自动复测
- B6 GEO 综合分
- decayed 自动改稿 opt-in 开关（现手动按钮，避免无 Key 空烧）
