---
type: cron
version: 1.0
updated: 2026-07-05
scope: "profile-lovart-management"
tools: [hermes, claude]
status: active
path: 1-1 Harness/06-cron/03-每月任务.md
generator: 1-1 Harness/11-knowledge/scripts/fm-fix.py
---
# 每月任务

Registry [附录 D](../ANTI-BUGS-REGISTRY.md#附录-d--运维节奏) 建议：每月或大批发布后做全量 HTTP 审计。

**工作目录**：

```bash
cd "/Users/seveno/Library/Mobile Documents/iCloud~md~obsidian/Documents/LifeOS Pro PARA Vault/1-Project/1-4 Dev/lovart.sanity.studio"
```

---

## 检查清单

| 任务 | 命令 | 目的 |
|------|------|------|
| Blog 封面全审计 | `node scripts/audit-blog-covers.js --check-http --report "$HOME/Documents/Lovart Local Dev/Temp/lovart/pull/blog-cover-audit-$(date +%Y%m).json"` | 404 / liblib 复发 |
| composite 图全审计 | `node scripts/audit-composite-images-404.js --report "$HOME/Documents/Lovart Local Dev/Temp/lovart/pull/composite-image-404-$(date +%Y%m).json"` | 同上 |
| 未来发布日期 | `node scripts/audit-blog-future-release-dates.js --report "$HOME/Documents/Lovart Local Dev/Temp/lovart/pull/blog-future-dates.json"` | AB-S07 |
| 线上内容质量 | `node scripts/audit-content-quality.js` | 占位符、zh 质量、EN 可读性 |
| SEO 月报 | `python3 "…/1-4 Dev/scripts/seo_monthly_v2.py" --month YYYY-MM` | OKR + 全板块 |
| taxonomy 同步（分类有变时） | `npx sanity exec scripts/sync-blog-taxonomy.js --with-user-token` | convert 写 ref 正确 |

---

## 归档

```bash
PULL="$HOME/Documents/Lovart Local Dev/Temp/lovart/pull"
DEST="$HOME/Documents/Lovart Local Dev/Output/quality-audits/$(date +%Y-%m)"
mkdir -p "$DEST"
cp "$PULL"/blog-cover-audit-*.json "$DEST/" 2>/dev/null || true
cp "$PULL"/composite-image-404-*.json "$DEST/" 2>/dev/null || true
cp "$PULL"/blog-future-dates.json "$DEST/" 2>/dev/null || true
```

---

## 每季度 / 低频率

| 任务 | 说明 |
|------|------|
| quality-audits 回顾 | 汇总 `~/Documents/Lovart Local Dev/Output/quality-audits/`，看 404 / 占位符是否复发 |
| SEO 季报 / 年报 | `seo_monthly_v2` 扩展窗口 |
| Secret 轮换检查 | 是否曾 `sanity deploy`、bundle 是否含 token — AB-E07 |
| 封面池 SSOT | 核对 `blog-cover-pool.js` 仍禁止 liblib — AB-I08 |

---

## 按场景触发（不必固定 cron）

| 场景 | 任务 |
|------|------|
| 换设备 / token 失效 | `check-sanity-auth.js --require-taxonomy` + `sync-blog-taxonomy` |
| 大批量 repair 后 | 查 `~/Documents/Lovart Local Dev/Temp/lovart/repair-state.json`，避免 AB-P06 重复修 |
| 新写 Blog MD 批次 | `preflight blog-md` + 可选 `audit-content-quality` |
| Features SEO 怀疑仍错 | GROQ 查 `/tools/` + `patch-composite-seo-urls-env.js --dry-run` |
