---
type: session-log
session_date: 2026-09-16
session_slug: p7-batch2
status: ready
---

# Session Log — P7 二批（竞品反向工程 / 复测窗口 / GEO 综合分 / 衰减自动改稿）

## 交付（全部 e2e + 合成单测）

1. **B2 竞品引用源反向工程**：geo_summary 新增 `competitor_urls`——竞品被提及时的引用 URL（剔除自家 canonical），按域聚合（被引次数/竞品/命中查询）→ GEO 卡「竞品引用源」列表 + 每域「→ 对标选题」按钮（选题自动带"对标 X 被引来源 + 命中查询"）。合成单测：g2.com 聚合 2 次、自家 URL 正确排除。
2. **B5 发布后复测窗口**：`recheck_window()`——发布 ≤7 天页面 × 引用记录 → impact.recheck（已被引用 ✓ / 待复测第 N 天）；分发页新卡呈现窗口状态。与 geo_scheduler 的每日自动探测天然联动（开了 probe_daily 即自动复测）。
3. **B6 GEO 综合分**：`geo_score()` 四因子——提及率 40% + 缺口覆盖 30% + 相对份额 20%（品牌 vs 品牌外提及）+ 结构健康 10%（衰减占比反向）→ 0-100。**诚实边界**：无探测数据返回 None 不造假分；iter 页 stat 卡 + GEO 卡大卡展示四因子分项。合成单测 51 分（各因子正确）。
4. **衰减自动改稿 opt-in**：meta.geo.auto_refresh 开关（GEO 卡 checkbox，title 明示会烧 token）→ geo_scheduler 每日探测后自动对 decayed 前 2 篇创建 refresh Loop（复用排程链，item_id 去重防重复烧钱，sched.log 记 DECAY-REFRESH 审计）。

## 踩坑
- renderGeoSummary 三连改漏行（竞品提及被后次 edit 吞掉）——两次都靠读回发现；改模板字符串段落要整段替换后整体核对
- impact/geo_score 里 brand_rate 作用域 bug（kwarg 传参处内联计算）——先赋局部变量再传

## 验证
- 服务器 e2e：impact.decayed 7 条 ✓ / recheck 0（近 7 天无新发布，诚实空）✓ / geo score None（无探测数据不造假）✓ / competitor_urls 0 ✓
- geo_score 合成单测（51 分四因子正确）、B2 聚合单测通过
- session-init GATE5 + 双端语法校验 ✓

## 至此 GEO LOOP 全形态
```
配置 → 每日多引擎探测 → 引用记录 → 归因（被引页面）
  ├─ 缺口查询 → 选题队列 / 改稿 Loop
  ├─ 竞品被引源 → 对标选题（B2）
  ├─ 衰减页 → 手动/自动改稿 Loop（opt-in，每日≤2）
  └─ 复测窗口（≤7 天新页）→ 自动复测
→ 自动排程生成（geo-check）→ 人工审 → 发布 → 综合分曲线 → 循环
```
