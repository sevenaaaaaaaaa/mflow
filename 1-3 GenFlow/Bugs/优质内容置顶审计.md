基于本次会话的完整记录，我先快速验证两个关键风险点（置顶边界是否被冲破、多语言混排规模），再给汇总。`/tmp/sanitytoken.txt` 已不在（会话切换清理了临时文件）。token 在 memory 里有备份，我写回再验证两个风险点：验证完成。两个风险点清晰了：

1. **置顶边界没被冲破**：pinned-list.json 里置顶文章边界在 `2026-05-09 ~ 2026-07-07` 区间，我们降权到 `2025-07-30` 远在其后（更早），不会挤掉置顶。
2. **多语言混排规模很大**：DE compositePage 60/301 采样含中文；JA blog 191/201 正文是日文（这是正常的，不是混排问题）——但 DE 标题中文残留（如 `"company 宣传册 设计 | Lovart"`）确实是质量问题，且**未被本次降权覆盖**（因为 description 不为空）。

下面是完整汇总：

---

# 📋 会话汇总：低质量内容降权排序

## 一、本次排查的质量问题（真实存在的）

| # | 问题类别 | 受影响 | 说明 |
|---|---------|--------|------|
| 1 | **Blog 缺 description** | 711 篇 | it:680, zh:28, en:2 |
| 2 | **Blog description 过短** (<10字符) | 58 篇 | 如 `"Введение\n"`, `"Сцена\n"` 等截断垃圾 |
| 3 | **Blog title 过短** (<10字符) | 15 篇 | 如 `"海螺AI評測"`, `"建立3D角色"` |
| 4 | **compositePage 缺 description** | 578 篇 | 各语言均有: pt:95, ru:92, zh-TW:90, ko:89... |
| 5 | **多语言混排 (未覆盖)** | DE 60+ 篇 | compositePage 标题含中文残留（如 `"company 宣传册 设计"`）|
| 6 | **薄内容 (已修复)** | 0 篇 | 此前批次已处理，本次扫描为 0 |
| 7 | **SEO模板污染 (已修复)** | 0 篇 | `"GSC search intent"` 等已清洗 |

> 注：问题 1~4 已被降权覆盖；问题 5 因 description 字段有值而**未被覆盖**（漏网）。

## 二、排查进度

```
[✅ 完成] 审计 8406 blog + 4965 compositePage (14维度探查)
[✅ 完成] 识别真实质量问题 (非误报, 去掉了"AI AI"全词匹配误判的4156篇)
[✅ 完成] 降权 1356 篇 → publishedAt=releaseDate=2025-07-30
[✅ 完成] 修复 compositePage 漏设 releaseDate (578篇)
[✅ 验证] 前端排序字段=releaseDate, 降权已生效
[⚠️ 未覆盖] 多语言混排 (DE标题中文残留) — 需另行处理
[⚠️ 未覆盖] desc 10~30字符的"凑数描述" — 阈值取舍问题
```

## 三、修复手段

1. **`quality_demote.py` v4 脚本**（纯服务端 GROQ，不拉 body，避免超时）
   - 审计条件：缺 description / desc<10字符 / title<10字符（blog）；缺 desc / desc<20字符（compositePage）
   - 降权：`patch` 同时设 `publishedAt` + `releaseDate` = `2025-07-30T08:00:00Z`
   - 分批 50/批，重试 3 次，0 失败
2. **补救脚本**（inline）：补设 578 篇 compositePage 的 `releaseDate`（因初版只设了 publishedAt，前端用 releaseDate 排序导致无效）

## 四、潜在风险

| 风险 | 等级 | 说明 |
|------|------|------|
| **置顶冲突** | 🟢 低 | 降权到 2025-07-30，置顶边界在 2026-05~07，未冲破 |
| **CDN 缓存延迟** | 🟡 中 | 前端排序可能 5~10 分钟才刷新，需观察线上 |
| **误伤正常内容** | 🟢 低 | 仅降权"缺desc/过短title"的明确劣质页，未波及正常页 |
| **多语言混排漏网** | 🟡 中 | DE 标题中文残留未被覆盖（desc 有值），仍污染 EN 列表页 |
| **desc 10~30字符凑数描述** | 🟡 中 | 阈值设在 <10，10~29字符的"短描述"未降权，仍影响列表卡片质量 |
| **恢复机制** | 🟡 中 | 降权后原始日期丢失，修复完需手动 `--apply` 恢复（脚本支持但需原始日期备份）|

## 五、还能做哪些

1. **补降权多语言混排**：DE compositePage 标题含中文的 60+ 篇（及全量扫描其他语言），单独降权或修复标题
2. **扩展 desc 阈值**：把 `desc < 30 字符` 也纳入降权（需先确认不是合理的短描述）
3. **恢复机制**：修复完低质量内容后，用 `quality_demote.py --apply` 反向操作（需先备份原始 publishedAt/releaseDate 到 JSON）
4. **自动化**：将审计+降权加入 `lovart-content-health-audit` 的 launchd 周检，形成"低质量自动沉底"闭环
5. **前端验证**：等 CDN 刷新后，实际访问 `/blog` 和 `/features` 列表页，确认降权页已沉底

---

**一句话结论**：1356 篇明确劣质内容已沉底到 2025-07-30，高质量内容浮到前面；但多语言混排（DE标题中文）和"凑数短描述"仍是漏网之鱼，建议下一批处理。


---

## Batch-2 续（2026-08-03）

**状态**：审计+备份完成；apply **被 Viewer token 403 拦截**。

| 项 | 结果 |
|----|------|
| DE 标题中文 | ✅ 仍为 0（8/2 修复有效） |
| it/pt/ru/fr 标题中文 | ⚠️ 226 composite 待降权 |
| blog desc 中文混入 | ⚠️ 7 篇待降权 |
| 章节名/MD 标题当 desc | ⚠️ 399 篇待降权 |
| 权限 | 🔴 sevena@lovart.ai = Viewer，无法 mutate |

详见 Local：`Output/QA-Memo/quality-demote-batch2-2026-08-03.md`
解锁后跑：`scripts/active/quality_demote_mixlang_apply.py --apply --tier P0`

**Apply 完成（2026-08-03）**：P0 451/451 + P1 181/181 → `2025-07-30T08:00:00Z`；验证 632/632。
