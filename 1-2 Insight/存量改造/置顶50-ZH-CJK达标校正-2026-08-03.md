# 置顶 50 ZH CJK 达标校正（2026-08-03）

## 结论

交接清单里的「11/48、Seedream/CivitAI 待修」**口径过时**。根因是同 slug 存在多份 ZH 文档：薄正文在 `zh-*`，加长版常在 `blog-*-zh` 或随机 ID；清单多数按薄副本计数，前端详情页却常读加长版。

本轮已做：
1. 修复 4 篇假封面 `9f5e1a2b…1280x800.png`（coverImage + body imageSource）→ 有效封面
2. 将 Seedream / CivitAI 加长 body 同步进置顶窗 `zh-*` 副本（保留 `publishedAt=2026-06-17`）
3. `nano-banana-pro-complete-guide` 扩写上传：`zh-nano-banana-pro-guide` **本地 CJK 10,053**（门禁 ≥10,000）

## 验证口径（以后统一）

1. 按 slug 取该语言**全部** twin，对每份 body 做 `span.text` 正则数 CJK，取 **max**
2. 用前端 HTML 确认实际服务的 `_id`（不要只信 `zh-*`）
3. `pt::text()` 长度只作粗筛；出门以本地正则为准
4. 封面必须 HEAD 200；同时查 `coverImage` 与首块 `imageSource.url`

## 重复文档模式

| 模式 | 示例 | 风险 |
|------|------|------|
| `blog-{slug}-zh` 加长 + `zh-{slug}` 薄 | seedream / civitai-red | 清单漏计加长版；置顶窗读薄版 |
| 随机 Sanity ID 加长 + `zh-*` 薄 | pixai / deepai / luma 等 | 同上 |
| 仅 `zh-*` 薄 | nano-banana-pro（扩写前） | 真实待修 |

铁律：不删 production 文档。策略是 **修封面 + 把合格 body 同步到前端实际读取的/置顶窗文档**。

## 本轮封面修复

| _id | 处理 |
|-----|------|
| blog-seedream-4-5-free-guide-zh | cover + body 首图 → 有效 CDN |
| blog-what-is-civitai-red-zh | cover → 有效 CDN |
| zh-leonardo-ai-review | cover + body 首图 |
| zh-pictory-ai-review | cover + body 首图 |

假封面特征：`…/9f5e1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f-1280x800.png`（HEAD 404）

## 进度重估（置顶相关 47 slug 抽样）

以 twin max + pt 粗筛（LIKELY_OK ≈ 已有加长 twin）：

- Seedream / CivitAI：**正文已达标**（本轮同步置顶窗副本）
- Nano Banana Pro：**本轮新达标**（10,053 CJK）
- 仍薄、需真扩写的典型：`free-vs-paid-ai-tools-compared`、`nano-banana-presentation-guide`、`complete-guide-brand-kit-every-industry`、`ai-design-for-saas`、`lovart-official-authentic-design` 等
- 有加长 twin 但需确认前端读哪份 + 封面是否 404：`pixai-review`、`deepai-review`、`imagefx-review`、`luma-dream-machine-review`、`freepik-ai-image-generator-review`、`pixverse-ai-review`、`adobe-firefly-review`、`complete-guide-consistent-ai-character-design`

下一 session 不要从 Seedream 重写起；先跑「前端 _id × 本地 CJK × 封面 HEAD」三联表，再只扩写真正 THIN 的 slug。

## 可靠上传路径

`~/Documents/Lovart Local Dev/scripts/active/zh_expand_uploader.py`

- 本地 `.txt`：第 1 行 title，第 2 行 description，正文 `##`/`###`
- 只 `createOrReplace`，禁止 `patch.set.body`
- 保留目标文档 `publishedAt` / `releaseDate` / `coverImage`

Nano 源稿目录：`~/Documents/Lovart Local Dev/Output/zh-expand/nano-banana-pro-complete-guide/full.txt`

## 前端缓存

Sanity 更新后详情页可能仍吐旧 H2（ISR/CDN）。以 Sanity 本地 CJK 为准；发布后隔一段时间再 curl 抽查关键句。
