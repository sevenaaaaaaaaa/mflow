# deploy/cf.env — Cloudflare API 凭证（复制为 cf.env 后填写；cf.env 被 .gitignore 排除）
# Token 权限：Zone → Cache Purge（仅 nownexts.com 这个 zone 即可）
# CF_ZONE_ID：CF 面板 → 域名概览页右侧 "Zone ID"
CF_API_TOKEN=""
CF_ZONE_ID=""
CF_DOMAIN="nownexts.com"
