# gh.env.example —— 复制为 deploy/gh.env 后填写（*.env 已被 .gitignore 忽略，不会入库）
#
# 作用：让**没有你 Mac 家目录访问权**的环境（如 Cowork 沙箱里的 agent）也能 push 到 GitHub。
# 你自己在 Mac 上跑 deploy/sync.sh 不需要这个文件——你的 gh CLI 凭据本来就能用。
#
# token 权限只需 repo:contents（写）。建议用 fine-grained PAT 并只授权 sevenaaaaaaaaa/mflow。
# 泄露风险控制：它只能动这一个私有仓库，不能碰服务器。

GH_USER=sevenaaaaaaaaa
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
